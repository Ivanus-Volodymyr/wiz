import React from 'react';

export default function CreateProjectLayout({ children }: { children: React.ReactNode }) {
  return <main className="p-[2%] lg:p-[7%] bg-[#BFC9D31A] h-full">{children}</main>;
}
